# 10ac xinhan 265 dm (Sanitized Export)

This export intentionally excludes sensitive session/auth artifacts:
- Excluded: tdata/, modules/, log.txt, *.exe
- Redacted from account JSON: app_hash, twoFA, id, names, direct phone number

Included:
- Per-account sanitized JSON
- manifest.json for inventory
